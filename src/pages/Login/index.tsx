import React, { ReactElement } from 'react'
import LoginFormComp from "../../container/LoginFormComp"

interface Props {
    
}

export default function index({}: Props): ReactElement {
    return (
        <LoginFormComp></LoginFormComp>
    )
}
