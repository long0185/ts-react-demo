import React, { ReactElement, useState } from 'react'
import MovieForm from "../components/MovieForm"

interface Props {

}

export default function Add({ }: Props): ReactElement {
    return (
        <MovieForm></MovieForm>
    )
}
