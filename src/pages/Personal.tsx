import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Personal({}: Props): ReactElement {
    return (
        <div>
            psersonal
        </div>
    )
}
